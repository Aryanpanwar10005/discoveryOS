# Python Parser — Reserved

Parser implementation currently lives under `discovery-frontend/python/` — see that folder for the actual extraction scripts (extract.py, pdf.py, docx_extract.py, etc.).

This top-level `python/` folder is reserved per the locked repo structure; consolidation TBD.
