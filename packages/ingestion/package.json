{
  "name": "@discoveryos/ingestion",
  "version": "0.0.1",
  "description": "Document ingestion service for DiscoveryOS",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "demo": "node dist/demo.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@discoveryos/core": "workspace:*",
    "franc": "^6.2.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
